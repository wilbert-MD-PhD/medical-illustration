# 验证范围

## 1.1.1 更新器增量验证（2026-09-14）

- 当前包为 1.1.1，37 个载荷文件，连同 SHA256SUMS 共 38 个文件。包清单、链接、Skill 元数据和版本一致性已核验。
- 更新器 28 项本地回归通过：完整替换/回退、旧版接入、真实 CLI 自身回退、本地修改保护、损坏包/内部清单、路径越界/符号链接/大小写冲突、并发锁及修改、失败自动恢复、恢复失败保留副本和锁、备份被改、禁止降级、重复更新、只读检查、多个副本、插件管理拒绝、Release 筛选与缺附件、联网更新模拟及6项限流认证回退测试（含403/429、缺CLI、失效登录、超时、日志不泄露与无须回退的情况）。
- 原有初始化器 12 项、发布工具 11 项、运行组件/关系 15 项通过，共 66 项。Skill 快速检查和 git diff --check 通过。新增测试已加入跨平台工作流；当前提交的三平台结果以 Actions 为准，本地结果不替代远端运行。
- 对本机实际全局安装做了只读检查：VERSION 缺失，legacy_install=true；现有文件与其自身清单匹配，但不能由此确认官方发布版本。没有替换该安装。
- 发布前检查曾复现匿名 GitHub API 403 限流；已补充已登录 GitHub CLI 只读回退，本次真实默认 CLI 检查已通过，返回 metadata_source=github-api（匿名API已恢复）。另以模拟403触发新分支，后续调用真实已登录gh读取公开Release成功，返回gh-cli；不将模拟触发写成自然限流。隔离环境中的默认联网 update 实际完成元数据查询、公开1.1.0 ZIP/校验文件下载、备份替换与 rollback，原文件哈希恢复。无模拟元数据或下载；本机全局安装保持原样。网络及认证均不可用时仍明确报错，不误报已是最新。
- 本次未重新运行 Illustrator 或渲染漫画，未修改相关生产代码和图稿。以下 1.1.0 宿主与视觉验收是历史依据，不冒充本次重测。
- 本节记录发布前完成的本地验收；是否已发布以1.1.1标签和Release为准。尚未验证新会话自动发现与Marketplace更新。

## 1.1.0 既有验收记录

检查日期：2026-09-14。对象为 1.1.0 发布源码；软件包、宿主执行、视觉检查和人工医学审签分别记录。

| 检查 | 结果 |
|---|---|
| 版本与载荷 | VERSION、初始化器、插件元数据、安装说明与发布文案统一为1.1.0；独立 Skill 含35个载荷文件，连同 SHA256SUMS 共36个文件 |
| 初始化器 | 12项测试通过：用户修改保护、中文/非UTF-8控制台、文件冲突、路径和模板署名 |
| 发布工具 | 11项测试通过：目录构建、不覆盖、Unicode ZIP临时往返、可重复性、篡改/越界/源目录保护及运行组件缺失拒绝 |
| 运行组件与关系格式 | 15项测试通过，包含6种关系的子用例、来源/节点缺失、假说虚线、重复ID；运行器用模拟宿主验证占锁、成功/报错、超时和未确认结果；可选裁切验证像素与原文件保护 |
| 独立目录 | 从唯一源码构建；载荷哈希与源码一致。本地验收使用普通目录；正式 ZIP 由标签工作流构建和校验，未执行全局安装 |
| Illustrator 排字与会话 | 在 Illustrator 29.4.0 实际新建两页、保存 AI/PDF、关闭并逐文件重开检查：2画板、6可编辑文本框、1嵌入图、0外链，原有标签保留，锁释放 |
| PDF 视觉 | 上述两页实际渲染查看；中文、Latin字距、换行、标签与半透明气泡无明显截断或重叠 |
| 腕骨示例 | 23项来源/成图文件哈希及15处展示裁切来源匹配；5幅解剖图与6处放置沿用已登记保留核验。原漫画及参考资产未因本次代码修复改变 |
| 双语主页与对照图 | 当前文件与2026-09-14已实际查看版本一致；五组对照图已逐图查看，中英文主页各18图成功加载，无横向溢出；称呼、状态按钮及参考展示保留 |

## 宿主验证的准确范围

本机项目要求使用既有串行适配器，因此实际派发通过该入口执行；测试加载了独立候选目录中的新版排字库。候选中的 `illustrator_session.jsx` 与实际使用的会话库字节相同，SHA-256 为 `de02ac517caf88f36cfe011f69205bb11e56d6b5d25beb85ee5bfe5c2c255ac6`。

随包 Python 适配器另做静态检查和模拟派发/锁回归；没有把“通过本机既有适配器”写成“在全新电脑逐步安装并执行了随包入口”。首次宿主运行已保存、关闭并检查成稿，但最后的测试记录使用了宿主不提供的 JSON 全局对象而报错；改用兼容记录器后，仅重开这份测试文件即完成核验。该记录器属于验收脚本，不是分发组件。

1.0.0 历史的两页宿主测试曾超时。本次排字库已接入显式会话，其成功验收属于1.1.0，不能倒填为旧版通过。测试为独立的非医学文字/图形页，不重建漫画主稿。

## 来源与医学范围

[腕骨示例](showcase/wrist/README.md)保存具体图版/书页、实际生成附件与逐项许可。主页新增展示复用此前核验的参考，没有把展示裁图登记成原始生成附件。两项动作研究本轮未重新完成全文核查；[来源记录](showcase/wrist/CREDITS.md)明确适用范围，示例仍为医学待审。

[创可贴漫画](showcase/bandage/README.md)和既有 TFCC 示例保持原有状态。文件、来源和视觉核验不替代解剖学全审、医学审签或印前批准。

## 远端与尚未实测的项目

工作流已加入运行组件测试。各提交的六项跨平台结果见[Validate](https://github.com/wilbert-MD-PhD/medical-illustration/actions/workflows/validate.yml)，正式标签的三平台验证、附件构建及下载核验见[Release](https://github.com/wilbert-MD-PhD/medical-illustration/actions/workflows/release.yml)。本地测试与远端结果分别记录，判断发布状态时核对具体提交及标签。

尚未实测：全新环境安装/自动发现、Marketplace、Windows/Linux原生 Illustrator 自动化、所有图像模型行为、第三方字体和插件组合。随包自动派发仅支持 macOS；其他平台可静态预检或使用 SVG/矢量路径。发布前本地通过不表示已经创建标签、上传附件或获得人工医学批准。

## 发布后轻量预览更新（2026-09-14）

轻量预览按阅读清晰度重新导出，515,274字节，链接标注0.52 MB。保留当前医生示例的六页和矢量文字，图片使用180dpi导出；全部六页实际查看，逐页文字和尺寸与来源PDF核对一致。此前72dpi版本因清晰度不足已替换。用户确认来源为《WR-002-腕骨与关节组成-Skill示例-v1.1.pdf》，实际共六页；当前预览包含该文件全部页面，没有删页或删对白。完整阅读稿及其余原始资产不变。
